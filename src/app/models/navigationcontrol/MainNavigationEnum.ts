export enum MainNavigationEnum {
  dashboard,
  browse,
  settings,
  login,
  register,
  logout,
  closeDialog,
  showToast
}

//Toolbar -> Input
//APP -> Output

// This enum will be used to control main navigation
