export class RentalInfo {
  constructor(public roomCount: number,
              public bedCount: number,
              public bathCount: number,
              public maxGuestsCount: number,
              public description: number) {
  }
}
